language: rust

rust:
  - stable
  - beta
  - nightly
  - 1.26.0

install:
  - cargo install cargo-tree

script:
  - (cargo tree --duplicate | grep ^crossbeam) && exit 1
  - ./ci/script.sh
  - (cd crossbeam-utils && ./ci/script.sh)
